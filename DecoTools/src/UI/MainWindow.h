// Pewpew's Deco Tools - Main Window Interface
// Declares the controls used to render, show, hide, and toggle the addon's main
// window and its Nexus options panel.

#pragma once

namespace MainWindow
{
    void Render();
    void RenderOptions();
    void SetWindowsVisible(bool visible);
    void ToggleWindows();
}
